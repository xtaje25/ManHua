@using ManHuaAdmin.Models;
@{
    ViewBag.Title = "AddView";
    List<Tab_GongZhongHao> list = ViewBag.list;
}

<div class="pageContent">
    <form method="post" action="@Url.Content("~/User/Add")" class="pageForm required-validate" onsubmit="return validateCallback(this, dialogAjaxDone);">
        <div class="pageFormContent" layoutH="56">
            <p>
                <label>关联公众号：</label>
                <select name="gid" ref="w_combox_city">
                    <option value="0">请选择</option>
                    @foreach (var it in list)
                    {
                        <option value="@it.F_Id">@it.F_GZHName</option>
                    }
                </select>
            </p>
            <p>
                <label>登录账号：</label>
                <input name="name" class="required" type="text" size="30" minlength="1" maxlength="50" />
            </p>
            <p>
                <label>登录密码：</label>
                <input name="pwd" type="text" size="30" readonly="readonly" value="123456" />
            </p>
            <p>
                <label>账号权限：</label>
                <input name="auth" type="text" size="30" readonly="readonly" value="公号管理员" />
            </p>
        </div>
        <div class="formBar">
            <ul>
                <li>
                    <div class="buttonActive"><div class="buttonContent"><button type="submit">保存</button></div></div>
                </li>
                <li>
                    <div class="button"><div class="buttonContent"><button type="button" class="close">取消</button></div></div>
                </li>
            </ul>
        </div>
    </form>
</div>

